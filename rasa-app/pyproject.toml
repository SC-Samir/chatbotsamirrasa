[project]
name = "scalingoops-rasa"
version = "0.1.0"
description = "Rasa service for ScalingoOps"
requires-python = ">=3.10,<3.11"
dependencies = [
  "rasa==3.6.21; platform_system == 'Linux'",
]

[tool.uv]
environments = ["sys_platform == 'linux'"]
